[project]
name = "agentic-construction-workpackage-reference"
version = "0.1.0"
description = "Synthetic reference pipeline for agentic construction work-package generation"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
